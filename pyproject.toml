[project]
name = "prepare-llm-training-dataset"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.5.0",
    "datasets>=5.0.1",
    "huggingface-hub>=1.31.0",
    "safetensors>=0.8.0",
    "tiktoken>=0.14.0",
    "torch>=2.14.0",
    "tqdm>=4.70.1",
]
